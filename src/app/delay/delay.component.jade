.container
  h3 delay
  .panel.panel-success
    .panel-heading 对比分析
      .list-group
  .panel.panel-info
    .panel-heading delay(延时) 说明
    .panel-body
      p 签名: delay(delay: number | Date, scheduler: Scheduler): Observable
      p 通过给定的超时或者直到一个给定的时间来延迟源 Observable 的发送
      p 每个数据项的发出时间都往后推移固定的毫秒数
      p 如果延时参数是数字, 这个操作符会将源 Observable 的发出时间都往后推移固定的毫秒数。 保存值之间的相对时间间隔. 如果延迟参数是日期类型, 这个操作符会延时Observable的执行直到到了给定的时间
      img.img-responsive.center-block(src="../assets/images/delay.png")
      pre.pre-scrollable.
        弹珠图解析:
        - 源 Observable 每次发出值, 都要延时 20 毫秒才输出
  .panel.panel-info
    .panel-heading 示例1: 弹珠图示例
    .panel-body
      pre.pre-scrollable([innerHtml]="demo1Info")
      p
        button.btn.btn-primary(type="button",
          [disabled]="isRuning",
          (click)="runDemo1();$event.stopPropagation()") 运行
