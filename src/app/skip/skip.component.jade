.container
  h3 skip
  .panel.panel-success
    .panel-heading 对比分析
      .list-group
  .panel.panel-info
    .panel-heading skip(按数量跳过) 说明
    .panel-body
      p 签名: skip(the: Number): Observable
      p 跳过N个(由参数提供)发出值
      p 返回一个 Observable， 该 Observable 跳过源 Observable 发出的前N个值(N = count)
      img.img-responsive.center-block(src="../assets/images/skip.png")
      pre.pre-scrollable.
        弹珠图解析:
  .panel.panel-info
    .panel-heading 示例1:
    .panel-body
      pre.pre-scrollable([innerHtml]="demo1Info")
      p
        button.btn.btn-primary(type="button",
          [disabled]="isRuning",
          (click)="runDemo1();$event.stopPropagation()") 运行
